{
  "name": "peercal",
  "dependencies": {
    "choo": "^7.1.0",
    "hyperdrive": "^10.21.0",
    "hyperswarm-web": "^2.2.0",
    "random-access-idb": "^1.2.2",
    "random-access-memory": "^3.1.4",
    "sheetify": "^7.4.0"
  },
  "devDependencies": {
    "ava": "^4.0.1",
    "budo": "^11.6.4",
    "indexhtmlify": "^2.0.1",
    "standard": "^16.0.4"
  },
  "browserify": {
    "transform": [
      "sheetify/transform"
    ]
  },
  "scripts": {
    "start": "npm run prepare && budo app.js:public/bundle.js -l -d public -p 7777",
    "start2": "budo app.js:public/bundle.js -l -d public -p 7777",
    "build": "npm run prepare && browserify app.js | indexhtmlify --title 'peercal' > public/index.html",
    "prepare": "cp -n config.default.json config.json && rm -rf public && mkdir public",
    "test": "standard && ava -v tests/*.test.js",
    "tdd": "ava -w -v tests/*.test.js"
  }
}
